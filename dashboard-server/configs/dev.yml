appId: "1234"

shouldStartCouchbaseServer: false

couchbase:
  clusters:
    defaultCluster:
      username: "admin"
      password: "123qweasd"
      host: "localhost"
      buckets:
        - "dashboard-data"

logging:
  level: INFO
  loggers:
    com.footballstatsdashboard: DEBUG